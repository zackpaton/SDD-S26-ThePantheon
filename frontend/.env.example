# Copy to `.env.local` and adjust. Public vars are exposed to the browser (NEXT_PUBLIC_*).
# Backend origin only — no trailing slash. Routes still use `/api/...` in code.
NEXT_PUBLIC_API_URL=http://localhost:3001
