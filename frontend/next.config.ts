/**
 * Next.js build configuration (e.g. dev indicator toggles); extend here for images, redirects, or env.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: false
};

export default nextConfig;
